# LaTeX Input Files for Tables

All tables should be generated programmatically (no typing in numbers by hand!) and should take the form of *.tex files that can be read in directly by the main document.

