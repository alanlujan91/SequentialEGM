This template is an unused placeholder. Docx exports pass jtex-style template options to a dynamic renderer, not a static template.

For now, the docx renderer uses:
[-CONTENT-]
[-options.hideFooter-]

Of course, docx does not need tex imports:
[-IMPORTS-]
