<article xmlns:mml="http://www.w3.org/1998/Math/MathML" xmlns:xlink="http://www.w3.org/1999/xlink" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:ali="http://www.niso.org/schemas/ali/1.0/" dtd-version="1.3" xml:lang="en">
  <body>
    <p>/SequentialEGM</p>
    <p>document</p>
    <sec id="sec:method">
      <title>The Sequential Endogenous Grid Method</title>
      <p>The baseline problem which I will use to demonstrate the Sequential Endogenous Grid Method (EGM
        <inline-formula>
          <tex-math><![CDATA[^n]]></tex-math>
        </inline-formula>) is a discrete time version of
        <xref ref-type="bibr" rid="Bodie1992">Bodie
          <italic>et al.</italic> (1992)
        </xref> where a consumer has the ability to adjust their labor as well as their consumption in response to financial risk. The objective consists of maximizing the present discounted lifetime utility of consumption and leisure.
      </p>
      <disp-formula>
        <label>(1)</label>
        <tex-math><![CDATA[\VFunc_0(\BLev_0, \tShkEmp_0) = \max \Ex_{t} \left[ \sum_{n = 0}^{T-t} \DiscFac^{n} \utilFunc(\CLev_{t+n}, \Leisure_{t+n})  \right]]]></tex-math>
      </disp-formula>
      <p>In particular, this example makes use of a utility function that is based on Example 1 in the paper, which is that of additively separable utility of labor and leisure as</p>
      <disp-formula>
        <label>(2)</label>
        <tex-math><![CDATA[\utilFunc(\CLev, \Leisure) = \util(\CLev) + \h(\Leisure) = \frac{C^{1-\CRRA}}{1-\CRRA} + \labShare^{1-\CRRA} \frac{\Leisure^{1-\leiShare}}{1-\leiShare}]]></tex-math>
      </disp-formula>
      <p>where the term
        <inline-formula>
          <tex-math><![CDATA[\labShare^{1-\CRRA}]]></tex-math>
        </inline-formula> is introduced to allow for a balanced growth path as in
        <xref ref-type="bibr" rid="Mertens2011">Mertens &amp; Ravn (2011)</xref>. The use of additively separable utility is ad-hoc, as it will allow for the use of multiple EGM steps in the solution process, as we’ll see later.
      </p>
      <p>This model represents a consumer who begins the period with a level of bank balances
        <inline-formula>
          <tex-math><![CDATA[\bRat_{t}]]></tex-math>
        </inline-formula> and a given wage offer
        <inline-formula>
          <tex-math><![CDATA[\tShkEmp_{t}]]></tex-math>
        </inline-formula>. Simultaneously, they are able to choose consumption, labor intensity, and a risky portfolio share with the objective of maximizing their utility of consumption and leisure, as well as their future wealth.
      </p>
      <p>The problem can be written in normalized recursive form
        <xref ref-type="fn" rid="o4n7sbirql"/> as
      </p>
      <disp-formula>
        <label>(4)</label>
        <tex-math><![CDATA[\begin{split}
    \vFunc_{t}(\bRat_{t}, \tShkEmp_{t}) & = \max_{\{\cRat_{t},
      \leisure_{t}, \riskyshare_{t}\}} \utilFunc(\cRat_{t}, \leisure_{t}) +
    \DiscFac \Ex_{t} \left[ \PGro_{t+1}^{1-\CRRA}
      \vFunc_{t+1} (\bRat_{t+1},
      \tShkEmp_{t+1}) \right] \\
    & \text{s.t.} \\
    \labor_{t} & = 1 - \leisure_{t} \\
    \mRat_{t} & = \bRat_{t} + \tShkEmp_{t}\labor_{t} \\
    \aRat_{t} & = \mRat_{t} - \cRat_{t} \\
    \Rport_{t+1} & = \Rfree + (\Risky_{t+1} - \Rfree)
    \riskyshare_{t} \\
    \bRat_{t+1} & = \aRat_{t} \Rport_{t+1} / \PGro_{t+1}
  \end{split}]]></tex-math>
      </disp-formula>
      <p>in which
        <inline-formula>
          <tex-math><![CDATA[\labor_{t}]]></tex-math>
        </inline-formula> is the time supplied to labor net of leisure,
        <inline-formula>
          <tex-math><![CDATA[\mRat_{t}]]></tex-math>
        </inline-formula> is the market resources totaling bank balances and labor income,
        <inline-formula>
          <tex-math><![CDATA[\aRat_{t}]]></tex-math>
        </inline-formula> is the amount of saving assets held by the consumer, and
        <inline-formula>
          <tex-math><![CDATA[\riskyshare_{t}]]></tex-math>
        </inline-formula> is the risky share of assets, which induce a
        <inline-formula>
          <tex-math><![CDATA[\Rport_{t+1}]]></tex-math>
        </inline-formula> return on portfolio that results in next period’s bank balances
        <inline-formula>
          <tex-math><![CDATA[\bRat_{t+1}]]></tex-math>
        </inline-formula> normalized by next period’s permanent income
        <inline-formula>
          <tex-math><![CDATA[\PGro_{t+1}]]></tex-math>
        </inline-formula>.
      </p>
      <sec id="restating-the-problem-sequentially">
        <title>Restating the problem sequentially</title>
        <p>We can make a few choices to create a sequential problem which will allow us to use multiple EGM steps in succession. First, the</p>
        <p>agent decides their labor-leisure trade-off and receives a wage. Their wage</p>
        <p>plus their previous bank balance then becomes their market resources. Second, given</p>
        <p>market resources, the agent makes a pure consumption-saving decision. Finally, given an amount of savings, the consumer then decides their risky portfolio share.</p>
        <p>Starting from the beginning of the period, we can define the labor-leisure problem as</p>
        <disp-formula>
          <label>(5)</label>
          <tex-math><![CDATA[\begin{split}
    \vFunc_{t}(\bRat_{t}, \tShkEmp_{t}) & = \max_{ \leisure_{t}}
    \h(\leisure_{t}) + \vOpt_{t} (\mRat_{t}) \\
    & \text{s.t.} \\
    0 & \leq \leisure_{t} \leq 1 \\
    \labor_{t} & = 1 - \leisure_{t} \\
    \mRat_{t} & = \bRat_{t} + \tShkEmp_{t}\labor_{t}.
  \end{split}]]></tex-math>
        </disp-formula>
        <p>The pure consumption-saving problem is then</p>
        <disp-formula>
          <label>(6)</label>
          <tex-math><![CDATA[\begin{split}
    \vOpt_{t}(\mRat_{t}) & = \max_{\cRat_{t}} \util(\cRat_{t}) + \DiscFac\vEnd_{t}(\aRat_{t}) \\
    & \text{s.t.} \\
    0 & \leq \cRat_{t} \leq \mRat_{t} \\
    \aRat_{t} & = \mRat_{t} - \cRat_{t}.
  \end{split}]]></tex-math>
        </disp-formula>
        <p>Finally, the risky portfolio problem is</p>
        <disp-formula>
          <label>(7)</label>
          <tex-math><![CDATA[\begin{split}
    \vEnd_{t}(\aRat_{t}) & = \max_{\riskyshare_{t}}
    \Ex_{t} \left[ \PGro_{t+1}^{1-\CRRA}
      \vFunc_{t+1}(\bRat_{t+1},
      \tShkEmp_{t+1}) \right] \\
    & \text{s.t.} \\
    0 & \leq \riskyshare_{t} \leq 1 \\
    \Rport_{t+1} & = \Rfree + (\Risky_{t+1} - \Rfree)
    \riskyshare_{t} \\
    \bRat_{t+1} & = \aRat_{t} \Rport_{t+1} / \PGro_{t+1}.
  \end{split}]]></tex-math>
        </disp-formula>
        <p>This sequential approach is explicitly modeled after the nested approaches explored in
          <xref ref-type="bibr" rid="Clausen2020">Clausen &amp; Strub (2020)</xref> and
          <xref ref-type="bibr" rid="Druedahl2021">Druedahl (2021)</xref>. However, I will offer additional insights that expand on these methods. An important observation is that now, every single choice is self-contained in a subproblem, and although the structure is specifically chosen to minimize the number of state variables at every stage, the problem does not change by this structural imposition. This is because there is no additional information or realization of uncertainty that happens between decisions, as can be seen by the expectation operator being in the last subproblem. From the perspective of the consumer, these decisions are essentially simultaneous, but a careful organization into sub-period problems enables us to solve the model more efficiently and can provide key economic insights. In this problem, as we will see, a key insight will be the ability to explicitly calculate the marginal value of wealth and the Frisch elasticity of labor.
        </p>
      </sec>
    </sec>
    <sec id="the-portfolio-decision-subproblem">
      <title>The portfolio decision subproblem</title>
      <p>As useful as it is to be able to use the EGM step more than once, there are clear problems where the EGM step is not applicable. This basic labor-portfolio choice problem demonstrates where we can use an additional EGM step, and where we can not. First, we go over a subproblem where we can not use the EGM step.</p>
      <p>In reorganizing the labor-portfolio problem into subproblems, we assigned the utility of leisure to the leisure-labor subproblem and the utility of consumption to the consumption-savings subproblem. There are no more separable convex utility functions to assign to this problem, and even if we re-organized the problem in a way that moved one of the utility functions into this subproblem, they would not be useful in solving this subproblem via EGM as there is no direct relation between the risky share of portfolio and consumption or leisure. Therefore, the only way to solve this subproblem is through standard convex optimization and root-finding techniques.</p>
      <p>Restating the problem in compact form gives</p>
      <disp-formula>
        <label>(8)</label>
        <tex-math><![CDATA[\vEnd_{t}(\aRat_{t}) = \max_{\riskyshare_{t}}  \Ex_{t} \left[ \PGro_{t+1}^{1-\CRRA}
  \vFunc_{t+1}\left(\aRat_{t}(\Rfree + (\Risky_{t+1} - \Rfree) \riskyshare_{t}), \tShkEmp_{t+1}\right)
  \right].]]></tex-math>
      </disp-formula>
      <p>The first-order condition with respect to the risky portfolio share is then</p>
      <disp-formula>
        <label>(9)</label>
        <tex-math><![CDATA[\Ex_{t} \left[ \PGro_{t+1}^{-\CRRA} \vFunc_{t+1}^{\bRat}\left(\bRat_{t+1}, \tShkEmp_{t+1}\right) (\Risky_{t+1} - \Rfree)  \right] = 0.]]></tex-math>
      </disp-formula>
      <p>Finding the optimal risky share requires numerical optimization and root-solving of the first-order condition. To close out the problem, we can calculate the envelope condition as</p>
      <disp-formula>
        <label>(10)</label>
        <tex-math><![CDATA[\vEnd_{t}'(\aRat_{t}) =  \Ex_{t} \left[ \PGro_{t+1}^{-\CRRA} \vFunc_{t+1}^{\bRat}\left(\bRat_{t+1}, \tShkEmp_{t+1}\right) \Rport_{t+1}  \right].]]></tex-math>
      </disp-formula>
      <sec id="a-note-on-avoiding-taking-expectations-more-than-once">
        <title>A note on avoiding taking expectations more than once.</title>
        <p>We could instead define the portfolio choice subproblem as:</p>
        <disp-formula>
          <label>(11)</label>
          <tex-math><![CDATA[\vEnd_{t}(\aRat_{t}) = \max_{\riskyshare_{t}} \vOptAlt(\aRat_{t}, \riskyshare_{t})]]></tex-math>
        </disp-formula>
        <p>where</p>
        <disp-formula>
          <label>(12)</label>
          <tex-math><![CDATA[\begin{split}
    \vOptAlt_{t}(\aRat_{t}, \riskyshare_{t}) & =  \Ex_{t} \left[ \PGro_{t+1}^{1-\CRRA} \vFunc_{t+1}\left(\bRat_{t+1}, \tShkEmp_{t+1}\right)   \right] \\
    \Rport_{t+1} & = \Rfree + (\Risky_{t+1} - \Rfree) \riskyshare_{t} \\
    \bRat_{t+1} & = \aRat_{t} \Rport_{t+1} / \PGro_{t+1}
  \end{split}]]></tex-math>
        </disp-formula>
        <p>In this case, the process is similar. The only difference is that we don’t have to take expectations more than once. Given the next period’s solution, we can calculate the marginal value functions as:</p>
        <disp-formula>
          <label>(13)</label>
          <tex-math><![CDATA[\begin{split}
    \vOptAlt_{t}^{\aRat}(\aRat_{t}, \riskyshare_{t}) & =  \Ex_{t} \left[ \PGro_{t+1}^{-\CRRA} \vFunc_{t+1}'\left(\bRat_{t+1}, \tShkEmp_{t+1}\right) \Rport_{t+1}   \right] \\
    \vOptAlt_{t}^{\riskyshare}(\aRat_{t}, \riskyshare_{t}) & =  \Ex_{t} \left[ \PGro_{t+1}^{-\CRRA} \vFunc_{t+1}'\left(\bRat_{t+1}, \tShkEmp_{t+1}\right) \aRat_{t} (\Risky_{t+1} - \Rfree)   \right] \\
  \end{split}]]></tex-math>
        </disp-formula>
        <p>If we are clever, we can calculate both of these in one step. Now, the optimal risky share can be found by the first-order condition and we can use it to evaluate the envelope condition.</p>
        <disp-formula>
          <label>(14)</label>
          <tex-math><![CDATA[\text{F.O.C.:} \qquad \vOptAlt_{t}^{\riskyshare}(\aRat_{t}, \riskyshare_{t}^{*})  = 0 \qquad
  \text{E.C.:} \qquad \vEnd_{t}^{\aRat}(\aRat_{t}) = \vOptAlt_{t}^{\aRat}(\aRat_{t}, \riskyshare_{t}^{*})]]></tex-math>
        </disp-formula>
      </sec>
    </sec>
    <sec id="the-consumption-saving-subproblem">
      <title>The consumption-saving subproblem</title>
      <p>The consumption-saving EGM follows
        <xref ref-type="bibr" rid="Carroll2006">Carroll (2006)</xref> but I will cover it for exposition. We can begin the solution process by restating the consumption-savings subproblem in a more compact form, substituting the market resources constraint and ignoring the no-borrowing constraint for now. The problem is:
      </p>
      <disp-formula>
        <label>(15)</label>
        <tex-math><![CDATA[\vOpt_{t}(\mRat_{t}) = \max_{\cRat_{t}} \util(\cRat_{t}) +
  \DiscFac \vEnd_{t}(\mRat_{t}-\cRat_{t})]]></tex-math>
      </disp-formula>
      <p>To solve, we derive the first-order condition with respect to
        <inline-formula>
          <tex-math><![CDATA[\cRat_{t}]]></tex-math>
        </inline-formula> which gives the familiar Euler equation:
      </p>
      <disp-formula>
        <label>(16)</label>
        <tex-math><![CDATA[\utilFunc'(\cRat_t) = \DiscFac \vEnd_{t}'(\mRat_{t} - \cRat_{t}) = \DiscFac
  \vEnd_{t}'(\aRat_{t})]]></tex-math>
      </disp-formula>
      <p>Inverting the above equation is the (first) EGM step.</p>
      <disp-formula>
        <label>(17)</label>
        <tex-math><![CDATA[\cEndFunc_{t}(\aRat_{t}) = \utilFunc'^{-1}\left( \DiscFac \vEnd_{t}'(\aRat_{t})
  \right)]]></tex-math>
      </disp-formula>
      <p>Given the utility function above, the marginal utility of consumption and its inverse are</p>
      <disp-formula>
        <label>(18)</label>
        <tex-math><![CDATA[\utilFunc'(\cRat) = \cRat^{-\CRRA} \qquad \utilFunc'^{-1}(\xRat) =
  \xRat^{-1/\CRRA}.]]></tex-math>
      </disp-formula>
      <p>
        <xref ref-type="bibr" rid="Carroll2006">Carroll (2006)</xref> demonstrates that by using an exogenous grid of
        <inline-formula>
          <tex-math><![CDATA[\aMat]]></tex-math>
        </inline-formula> points we can find the unique
      </p>
      <p>
        <inline-formula>
          <tex-math><![CDATA[\cEndFunc_{t}(\aMat)]]></tex-math>
        </inline-formula> that optimizes the consumption-saving problem, since the first-order condition is necessary and sufficient.
      </p>
      <p>Further, using the market resources constraint, we can recover the exact amount</p>
      <p>of market resources that is consistent with this consumption-saving decision as</p>
      <disp-formula>
        <label>(19)</label>
        <tex-math><![CDATA[\mEndFunc_{t}(\aMat) = \cEndFunc_{t}(\aMat) + \aMat.]]></tex-math>
      </disp-formula>
      <p>This
        <inline-formula>
          <tex-math><![CDATA[\mEndFunc_{t}(\aMat)]]></tex-math>
        </inline-formula> is the “endogenous” grid that is consistent
      </p>
      <p>with the exogenous decision grid
        <inline-formula>
          <tex-math><![CDATA[\aMat]]></tex-math>
        </inline-formula>. Now that we have a
      </p>
      <p>
        <inline-formula>
          <tex-math><![CDATA[(\mEndFunc_{t}(\aMat), \cEndFunc_{t}(\aMat))]]></tex-math>
        </inline-formula> pair for each
      </p>
      <p>
        <inline-formula>
          <tex-math><![CDATA[\aRat \in \aMat]]></tex-math>
        </inline-formula>, we can construct an interpolating consumption function for
      </p>
      <p>market resources points that are off-the-grid.</p>
      <p>The envelope condition will be useful in the next section, but for completeness</p>
      <p>is defined here.</p>
      <disp-formula>
        <label>(20)</label>
        <tex-math><![CDATA[\vOpt_{t}'(\mRat_{t}) = \DiscFac \vEnd_{t}'(\aRat_{t}) = \utilFunc'(\cRat_{t})]]></tex-math>
      </disp-formula>
    </sec>
    <sec id="the-labor-leisure-subproblem">
      <title>The labor-leisure subproblem</title>
      <p>The labor-leisure subproblem can be restated more compactly as:</p>
      <disp-formula>
        <label>(21)</label>
        <tex-math><![CDATA[\vFunc_{t}(\bRat_{t}, \tShkEmp_{t}) = \max_{ \leisure_{t}}
  \h(\leisure_{t}) + \vOpt_{t}(\bRat_{t} +
  \tShkEmp_{t}(1-\leisure_{t}))]]></tex-math>
      </disp-formula>
      <p>The first-order condition with respect to leisure implies the labor-leisure Euler equation</p>
      <disp-formula>
        <label>(22)</label>
        <tex-math><![CDATA[\h'(\leisure_{t}) =	\vOpt_{t}'(\mRat_{t}) \tShkEmp_{t}]]></tex-math>
      </disp-formula>
      <p>The marginal utility of leisure and its inverse are</p>
      <disp-formula>
        <label>(23)</label>
        <tex-math><![CDATA[\h'(\leisure) = \labShare\leisure^{-\leiShare} \qquad
  \h'^{-1}(\xRat) = (\xRat/\labShare)^{-1/\leiShare}]]></tex-math>
      </disp-formula>
      <p>Using an exogenous grid of
        <inline-formula>
          <tex-math><![CDATA[\mMat]]></tex-math>
        </inline-formula> and
        <inline-formula>
          <tex-math><![CDATA[\tShkMat]]></tex-math>
        </inline-formula>, we can find leisure as
      </p>
      <disp-formula>
        <label>(24)</label>
        <tex-math><![CDATA[\zEndFunc_{t}(\mMat, \tShkMat) = \h'^{-1}\left(
  \vOpt_{t}'(\mMat) \tShkMat \right)]]></tex-math>
      </disp-formula>
      <p>In this case, it’s important to note that there are conditions for leisure itself. An agent with a small level of market resources
        <inline-formula>
          <tex-math><![CDATA[\mRat_{t}]]></tex-math>
        </inline-formula> might want to work more than their available time endowment, especially at higher levels of income
        <inline-formula>
          <tex-math><![CDATA[\tShkEmp_{t}]]></tex-math>
        </inline-formula>, if the utility of leisure is not enough to compensate for their low wealth. In these situations, the optimal unconstrained leisure might be negative, so we must impose a constraint on the optimal leisure function. This is similar to the treatment of an artificial borrowing constraint in the pure consumption subproblem. From now on, let’s call this constrained optimal function
        <inline-formula>
          <tex-math><![CDATA[\hat{\zEndFunc}_{t}(\mMat, \tShkMat)]]></tex-math>
        </inline-formula>, where
      </p>
      <disp-formula>
        <label>(25)</label>
        <tex-math><![CDATA[\hat{\zEndFunc}_{t}(\mMat, \tShkMat) = \max \left[ \min \left[ \zEndFunc_{t}(\mMat, \tShkMat), 1 \right], 0 \right]]]></tex-math>
      </disp-formula>
      <p>Then, we derive labor as
        <inline-formula>
          <tex-math><![CDATA[\lEndFunc_{t}(\mRat_{t}, \tShkEmp_{t}) = 1 -
  \hat{\zEndFunc}_{t}(\mRat_{t}, \tShkEmp_{t})]]></tex-math>
        </inline-formula>. Finally, for each
      </p>
      <p>
        <inline-formula>
          <tex-math><![CDATA[\tShkEmp_{t}]]></tex-math>
        </inline-formula> and
      </p>
      <p>
        <inline-formula>
          <tex-math><![CDATA[\mRat_{t}]]></tex-math>
        </inline-formula> as an exogenous grid, we can find the endogenous grid of bank
      </p>
      <p>balances as
        <inline-formula>
          <tex-math><![CDATA[\bEndFunc_{t}(\mRat_{t}, \tShkEmp_{t}) = \mRat_{t} -
  \tShkEmp_{t}\lEndFunc_{t}(\mRat_{t}, \tShkEmp_{t})]]></tex-math>
        </inline-formula>.
      </p>
      <p>The envelope condition then provides a heterogeneous Frisch elasticity of labor as simply</p>
      <disp-formula>
        <label>(26)</label>
        <tex-math><![CDATA[\vFunc_{t}^{b}(\bRat_{t}, \tShkEmp_{t}) = \vOpt_{t}'(\mRat_{t}) =
  \h'(\leisure_{t})/\tShkEmp_{t}.]]></tex-math>
      </disp-formula>
    </sec>
    <sec id="curvilinear-grids">
      <title>Curvilinear Grids</title>
      <p>Although EGM
        <inline-formula>
          <tex-math><![CDATA[^n]]></tex-math>
        </inline-formula> seems to be a simple approach, there is one important caveat that we have not discussed, which is the details of the interpolation. In the pure consumption-savings problem, a one-dimensional exogenous grid of post-decision liquid assets
        <inline-formula>
          <tex-math><![CDATA[\aMat]]></tex-math>
        </inline-formula> results in a one-dimensional endogenous grid of total market resources
        <inline-formula>
          <tex-math><![CDATA[\mMat]]></tex-math>
        </inline-formula>. However, as we know from standard EGM, the spacing in the
        <inline-formula>
          <tex-math><![CDATA[\mMat]]></tex-math>
        </inline-formula> grid is different from the spacing in the
        <inline-formula>
          <tex-math><![CDATA[\aMat]]></tex-math>
        </inline-formula> grid as the inverted Euler equation is non-linear. This is no problem in a one-dimensional problem as we can simply use non-uniform linear interpolation.
      </p>
      <p>However, the same is true of higher dimensional problems, where the exogenous grid gets mapped to a warped endogenous grid. In this case, it is not possible to use standard multi-linear interpolation, as the resulting endogenous grid is not rectilinear. Instead, I introduce a novel approach to interpolation that I call Warped Grid Interpolation (WGI), which is similar to
        <xref ref-type="bibr" rid="White2015">White (2015)</xref>’s approach but computationally more efficient and robust. The details of this interpolation method will be further explained in Section 
        <xref ref-type="sec" rid="sec:gpr">??</xref>, but for now, we show the resulting warped endogenous grid for the labor-leisure problem.
      </p>
    </sec>
    <sec id="alternative-parametrization">
      <title>Alternative Parametrization</title>
      <p>An alternative formulation for the utility of leisure is to state it in terms</p>
      <p>of the disutility of labor as in (references)</p>
      <disp-formula>
        <label>(27)</label>
        <tex-math><![CDATA[\h(\labor) = - \leiShare \frac{\labor^{1+\labShare}}{1+\labShare}]]></tex-math>
      </disp-formula>
      <p>In this case, we can restate the problem as</p>
      <disp-formula>
        <label>(28)</label>
        <tex-math><![CDATA[\h(\leisure) = - \leiShare
  \frac{(1-\leisure)^{1+\labShare}}{1+\labShare}]]></tex-math>
      </disp-formula>
      <p>The marginal utility of leisure and its inverse are</p>
      <disp-formula>
        <label>(29)</label>
        <tex-math><![CDATA[\h'(\leisure) = \leiShare(1-\leisure)^{\labShare} \qquad
  \h'^{-1}(\xRat) = 1 - (\xRat/\leiShare)^{1/\labShare}]]></tex-math>
      </disp-formula>
      <p>Web</p>
      <p>[figure]list=no</p>
      <p>[table]list=no</p>
      <p>document</p>
    </sec>
  </body>
  <back>
    <fn-group>
      <fn id="o4n7sbirql">
        <label>o4n7sbIrqL</label>
        <p>As in
          <xref ref-type="bibr" rid="Carroll2009">Carroll (2009)</xref>, where the utility of normalized consumption and leisure is defined as
        </p>
        <disp-formula>
          <label>(3)</label>
          <tex-math><![CDATA[\utilFunc(\cRat_{t}, \leisure_{t}) = \PLev_{t}^{1-\CRRA} \frac{\cRat_{t}^{1-\CRRA}}{1-\CRRA} + (\labShare\PLev_{t})^{1-\CRRA} \frac{\leisure_{t}^{1-\leiShare}}{1-\leiShare}]]></tex-math>
        </disp-formula>
      </fn>
    </fn-group>
  </back>
</article>
